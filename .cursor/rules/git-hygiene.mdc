---
description: Git safety and change hygiene for agent work
alwaysApply: true
---

# Git Hygiene

- Treat the working tree as shared with the user. Never revert, overwrite, or clean up changes you did not make.
- Before committing, inspect status, diff, and recent log messages. Commit only when explicitly asked.
- Keep commits scoped to the requested work. Do not include unrelated generated files, secrets, or local config.
- Do not run destructive git commands such as hard reset, force push, or checkout-over-files unless explicitly approved.
- If hooks modify files, inspect the changes before proceeding.
- Mention remaining dirty files only when they are relevant to the task or commit.
