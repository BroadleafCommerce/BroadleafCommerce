#This pattern defines how the initial applicationContext sources are merged.
#This is a simple override mechanism - similar in behavior to the default way
#spring merges multiple applicationContext files.
action.default=REPLACE
matcher.default=ID